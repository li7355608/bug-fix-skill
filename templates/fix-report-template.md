# 修复报告

## 1. Bug 描述

| 项目 | 内容 |
|------|------|
| 报告人 | ${报告人} |
| 现象描述 | ${用户原始描述或错误信息} |
| 影响范围 | ${受影响的功能 / 接口 / 页面} |
| 严重程度 | ${严重 / 一般 / 轻微} |

## 2. 根因分析

- **问题定位**：`${类名}#${方法名}`
- **根本原因**：${详细说明为什么会出现这个 Bug}
- **触发条件**：${在什么条件下会触发该 Bug}

## 3. 复现测试

| 项目 | 内容 |
|------|------|
| 测试类 | `${测试类全限定名}` |
| 测试方法 | `${should_xxx_when_xxx}` |
| 修复前执行结果 | **失败** —— ${失败原因，如：expected <X> but was <Y>} |
| 修复后执行结果 | **通过** |

## 4. 修复方案

### 改动文件清单

| 序号 | 文件路径 | 改动说明 |
|------|----------|----------|
| 1 | `${文件路径}` | ${这个文件改了什么、为什么要改} |
| 2 | `${文件路径}` | ${这个文件改了什么、为什么要改} |

### 改动要点

${简要概括修复思路，一两句话说清楚}

## 5. 回归测试结果

| 项目 | 内容 |
|------|------|
| 测试范围 | ${跑了哪些模块 / 包的测试} |
| 测试总数 | ${N} |
| 通过数量 | ${N} |
| 失败数量 | ${N} |
| 跳过数量 | ${N} |

### 失败用例说明（如有）

${如果有失败的测试，逐个说明是已有问题还是本次修复引入的}

## 6. 风险评估

| 风险项 | 评估 |
|--------|------|
| 是否影响其他模块 | ${是/否，如果是请说明} |
| 是否需要修改配置 | ${是/否，如果是请说明} |
| 是否需要数据迁移 | ${是/否，如果是请说明} |
| 是否需要通知前端 / 调用方 | ${是/否，如果是请说明} |
| 多环境兼容性 | ${是否所有环境都兼容此修复} |

---

> 本报告由 AI 辅助生成，请开发人员确认后再合并代码。
